[project]
name = "wiz-mcp"
version = "0.0.4"
description = "A sample mcp server"
readme = "README.md"
authors = [
    { name = "Gentleelephant", email = "moon0hello@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.11.14",
    "hatchling>=1.27.0",
    "mcp[cli]>=1.5.0",
    "setuptools>=78.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
