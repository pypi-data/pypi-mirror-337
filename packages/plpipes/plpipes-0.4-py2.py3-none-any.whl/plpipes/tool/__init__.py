"""
This package provides a collection of tools that can be utilized via the command line.

You can run them using the following command:

```shell
python -m plpipes.tool.foo [arguments]
```
"""
