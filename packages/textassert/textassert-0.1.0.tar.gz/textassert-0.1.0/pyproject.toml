[project]
name = "textassert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Travior", email = "30752199+Travior@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.11.14",
    "click>=8.1.8",
    "pydantic>=2.10.6",
    "rich>=13.9.4",
]

[project.scripts]
textassert = "textassert:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
