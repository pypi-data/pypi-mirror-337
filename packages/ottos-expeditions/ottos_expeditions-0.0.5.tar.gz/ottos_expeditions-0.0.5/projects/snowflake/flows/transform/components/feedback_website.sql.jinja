WITH feedback_website AS (
    SELECT
        *
    FROM
        {{ ref(
            "read_feedback_website",
            flow="extract-load",
            reshape={
                "time": {
                    "column": "timestamp",
                    "granularity": "month"
                }
            }
        ) }}
)
SELECT
    *
FROM
    feedback_website
ORDER BY
    'TIMESTAMP' DESC
