[project]
name = "sqlite-mcp"
version = "0.1.0"
description = "A lightweight Model Context Protocol server for allowing LLMs to autonomously interact with SQLite database."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]==1.6.0",
    "python-multipart",
]
