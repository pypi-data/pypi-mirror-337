INSERT INTO {{ sink_table }} ({% for column_name in sink_columns -%}"{{ column_name }}"{{ "," if not loop.last else ""}}{% endfor %})
SELECT
	{% for column_name in raw_columns -%}content:{{ column_name }}{{ "," if not loop.last else ""}}{% endfor %}
FROM {{ source_table }};
