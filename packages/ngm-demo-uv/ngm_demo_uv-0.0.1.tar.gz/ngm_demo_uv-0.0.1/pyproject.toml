[project]
name = "ngm-demo-uv"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aibulat", email = "ngmaibulat@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
demo = "ngm_demo_uv:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
