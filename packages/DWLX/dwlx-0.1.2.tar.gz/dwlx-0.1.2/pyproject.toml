[build-system]
requires = ["flit"]
build-backend = "flit.buildapi"

[project]
name = "DWLX"
version = "0.1.2"
description = "Initialize python tools to avoid platform errors"
authors = [{ name = "Guillaume", email = "guillaume.plagier@example.com" }]
license = { file = "LICENSE" }
dependencies = [
    "requests"
]
readme = "README.md"
