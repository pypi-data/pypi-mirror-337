[project]
name = "agent-toolkit"
version = "0.1.9"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Andre Ratzenberger", email = "andre.ratzenberger@whiteduck.de" }
]
requires-python = ">=3.10"
dependencies = [
    "flock-core>=0.3.23",
    "rich>=13.9.4",
    "tomli>=2.0.1",
]

[project.scripts]
agent-toolkit = "agent_toolkit:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
