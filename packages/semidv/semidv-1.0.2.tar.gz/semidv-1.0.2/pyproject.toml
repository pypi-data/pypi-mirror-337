[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "semidv"
version = "1.0.2"
authors = [
  { name="Chien-Ting Tung", email="cttung@berkeley.edu" }
]
description = "This is SEMIDV: a semiconductor device simulator"
readme = "README.md"
requires-python = ">=3.6"
license = { file="LICENSE" }
dependencies = [
    "scipy",
    "cython",
    "fdint"
]

