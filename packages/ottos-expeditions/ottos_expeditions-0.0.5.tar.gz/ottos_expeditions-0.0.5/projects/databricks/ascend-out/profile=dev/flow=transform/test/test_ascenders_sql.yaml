component:
  flow_name: transform
  metadata:
    source:
      path: flows/transform/tests/test_ascenders_sql.sql
  name: test_ascenders_sql
  singular_test:
    inputs:
    - flow: transform
      name: ascenders
    severity: error
    sql: |-
      SELECT
          *
      FROM
          {{ ref("ascenders") }}
      WHERE
          id IN (
              SELECT
                  "1" as id
          )