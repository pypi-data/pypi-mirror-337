[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "bitcoinlib-dev"
version = "0.4.20"
description = "bitcoinlib database fixes ValueError: Old database version found (<0.4.20). Can not convert to >0.5 version database automatically, use updatedb.py tool to update"
authors = ["Gordon Lawson <gordonlawson@gmail.com>"]
license = "MIT"
readme = "README.md"
homepage = "https://github.com/gordonxdev/bitcoinlib-dev"
repository = "https://github.com/gordonxdev/bitcoinlib-dev"
keywords = ["bitcoin", "wallet", "cli", "cryptocurrency"]

[project]
name = "bitcoinlib-dev"
version = "0.4.20"
description = "bitcoinlib database fixes ValueError: Old database version found (<0.4.20). Can not convert to >0.5 version database automatically, use updatedb.py tool to update"
authors = [
    {name = "Gordon Lawson", email = "gordonlawson@gmail.com"}
]
dependencies = [
    "bitcoinlib>0.6.0",
    "click>=8.1.3,<9.0.0",
    "rich>=13.3.2,<14.0.0",
    "shutilwhich>=1.1.0,<2.0.0"
]
requires-python = ">=3.8"

[tool.poetry.dependencies]
python = "^3.8"
bitcoinlib = "^0.6.0"
click = "^8.1.3"
rich = "^13.3.2"
shutilwhich = "^1.1.0"

[tool.poetry.scripts]
clw = "bitcoinlib_dev.cli:main"

[project.scripts]
clw = "bitcoinlib_dev.cli:main"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"
pytest-cov = "^4.0.0"
black = "^22.12.0"
isort = "^5.11.4"
mypy = "^0.991"

# Corrected packages configuration as an array
[[tool.poetry.packages]]
include = "bitcoinlib_dev"
from = "src"
