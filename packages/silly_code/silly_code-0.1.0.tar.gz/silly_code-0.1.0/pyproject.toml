[project]
authors = [{name = "Jared Galloway", email = "jaredgalloway07@gmail.com"}]
dependencies = ["numpy"]
name = "silly_code"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
silly_code = { path = ".", editable = true }

[tool.pixi.tasks]
