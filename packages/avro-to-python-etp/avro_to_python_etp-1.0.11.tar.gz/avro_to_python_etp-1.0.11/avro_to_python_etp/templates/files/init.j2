{%- if comment -%}
""" init file for file imports at namespace level """
{% endif -%}
{#- from {{pip_import}}.{{file}} import {{file.name}} -#}
{#- from {% if not namespace.startswith('.') -%}{{pip_import}}.{%- endif -%}{{namespace}}{%- if file!='*' -%}.{{file|snake_case}}{%- endif %} import {{file}} -#}
{#- {%- for file in imports -%} 
from {% if not namespace.startswith('.') -%}{{pip_import}}.{%- endif -%}{{namespace}}{%- if file!='*' -%}.{{file|snake_case}}{%- endif %} import {{file}} 
{% endfor -%}
{%- for file in imports_local -%} 
from {{namespace}} import {{file}}
{% endfor -%} -#}