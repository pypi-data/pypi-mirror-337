[project]
authors = [{name = "Jared Galloway", email = "jaredgalloway07@gmail.com"}]
dependencies = ["numpy"]
name = "matsengrp-meeting"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
matsengrp_meeting = { path = ".", editable = true }

[tool.pixi.tasks]
