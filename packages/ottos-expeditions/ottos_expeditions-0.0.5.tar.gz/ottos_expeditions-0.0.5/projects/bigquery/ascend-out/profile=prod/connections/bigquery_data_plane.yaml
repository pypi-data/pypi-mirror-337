connection:
  bigquery:
    create_dataset_if_not_exists: true
    dataset: ${parameters.bigquery.dataset}
    location: ${parameters.bigquery.location}
    project: ${parameters.gcp.project_id}
  metadata:
    source:
      path: connections/bigquery_data_plane.yaml
  name: bigquery_data_plane