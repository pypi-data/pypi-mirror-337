[project]
authors = [{name = "Jared Galloway", email = "jaredgalloway07@gmail.com"}]
dependencies = ["numpy"]
name = "matsengrp_super_ai"
requires-python = ">= 3.11"
version = "0.1.0"
scripts = {generate-wisdom = "matsengrp_super_ai:main"}

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
matsengrp_super_ai = { path = ".", editable = true }

[tool.pixi.tasks]
