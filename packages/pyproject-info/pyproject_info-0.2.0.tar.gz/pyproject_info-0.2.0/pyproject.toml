[project]
name = "pyproject-info"
version = "0.2.0"
description = "A small library to read pyproject.toml files"
authors = [
    {name = "halon176",email = "acc.github@halon.cc"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
packages = [{include = "pyproject_info", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
