from setuptools import setup, find_packages

setup(
    name="parallel-brain",
    version="1.0.3",
    description="A brain-inspired parallel neural network framework",
    author="Darshan",
    packages=find_packages(),
    install_requires=["numpy"],
)
