{%- macro get_json_schema(schema) -%}
avro_schema: typing.Final[str] = '{{ json.dumps(schema) }}'
{# {{'schema: typing.ClassVar[str] = """\n' + json.dumps(schema)|compress + '\n"""'}} #}
{%- endmacro -%}

{%- block schema -%}
{{get_json_schema(file.expanded_schema)}}
{%- endblock -%}
