============
Installation
============

`holisticai-sdk` is avaiable on PyPI. You can install the library with `pip` using the following command:

.. code-block::

  # Run assessments in a self-contained environment
  pip install holisticai-sdk

  # Run assessments in your local environment
  pip install holisticai-sdk[full]

.. toctree::
    :maxdepth: 2
    :titlesonly:

    install