[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcp-greeting"
version = "0.1.3"
description = "A simple greeting MCP server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
]

[project.scripts]
mcp-greeting = "mcp_greeting:main"
