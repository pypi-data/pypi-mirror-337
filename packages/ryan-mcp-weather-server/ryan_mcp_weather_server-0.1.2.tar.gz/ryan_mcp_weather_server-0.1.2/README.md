# MCP Weather

Configure
```json
{
  "mcpServers": {
    "ryan_mcp_weather_server": {
      "command": "node",
      "args": ["path/to/build/index.js"],
      "env": {
        "JIRA_URL": "https://your-domain.atlassian.net",
      }
    }
  }
}
```
