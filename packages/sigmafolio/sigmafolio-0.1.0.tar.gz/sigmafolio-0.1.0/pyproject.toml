[project]
name = "sigmafolio"
version = "0.1.0"
description = "All your financial data in one place"
authors = [
    {name = "Ivan Georgiev",email = "ivan.georgiev@example.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"
ruff = "^0.11.2"
poetry = "^2.1.1"

[tool.poetry]
packages = [{include = "sigmafolio", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
