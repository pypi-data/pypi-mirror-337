[project]
name = "processional"
version = "0.1.4"
description = "multiprocess and multithread functional programming library"
authors = [{name = "jimy-byerley", email = "jimy.byerley@gmail.com"}]
license = "LGPL-3.0"
readme = "readme.md"
requires-python = ">=3.9"
dependencies = [
    "dill (>=0.3.9,<0.4.0)"
]

[tool.poetry]

[tool.poetry.group.test.dependencies]
numpy = ">=1.10"
pnprint = "^1.1"
tyro = "^0.8"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
