from setuptools import setup, find_packages

setup(
    name="nlp_lab_final",  # Change to your package name
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="A custom NLP package",
)
