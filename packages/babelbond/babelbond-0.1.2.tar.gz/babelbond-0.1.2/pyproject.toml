[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "babelbond"
version = "0.1.2"
description = "BabelBond CLI"
readme = "README.rst"
authors = [
  {name = "BabelBond", email = "contact@babelbond.com"}
]
maintainers = [
  {name = "BabelBond", email = "contact@babelbond.com"}
]
classifiers = [

]
license = {text = "GNU General Public License v3"}
dependencies = [
  "typer"
]

[project.optional-dependencies]
dev = [
    "coverage",  # testing
    "mypy",  # linting
    "pytest",  # testing
    "ruff"  # linting
]

[project.urls]

bugs = "https://github.com/babelbond/babelbond/issues"
changelog = "https://github.com/babelbond/babelbond/blob/master/changelog.md"
homepage = "https://github.com/babelbond/babelbond"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
"*" = ["*.*"]




# Mypy
# ----

[tool.mypy]
files = "."

# Use strict defaults
strict = true
warn_unreachable = true
warn_no_return = true

[[tool.mypy.overrides]]
# Don't require test functions to include types
module = "tests.*"
allow_untyped_defs = true
disable_error_code = "attr-defined"

