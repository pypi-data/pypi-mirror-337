[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "libfinance"
version = "0.0.6"

dependencies = ["lz4","msgpack","pandas"]
description = "libfinance is a modern finance API"
readme = "README.md"