[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ghiblify"
version = "0.0.1"
description = "Ghiblifies your images"
authors = [{ name = "cblagden", email = "chase.blagden@gmail.com" }]
