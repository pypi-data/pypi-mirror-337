[project]
name = "uuid-extension"
version = "0.0.0"
description = "UUID Extensions for Python"
readme = "README.md"
authors = [{ name = "Markus Feiks", email = "pypi.packages@feyx.de" }]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
