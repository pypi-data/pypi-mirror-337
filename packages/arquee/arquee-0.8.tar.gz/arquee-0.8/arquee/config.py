from os.path import join

base_folder = "arquee_utils"
logs_folder = join(base_folder, "logs")
datalake = join(base_folder, "data", "datalake")
warehouse = join(base_folder, "data", "warehouse")
