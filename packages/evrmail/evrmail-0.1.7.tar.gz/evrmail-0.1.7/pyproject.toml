[project]
name = "evrmail"
version = "0.1.7"
description = "Encrypted messaging over Evrmore"
authors = [{ name = "Phoenix" }]
dependencies = [
  "typer[all]",
  "coincurve",
  "base58",
  "pycryptodome",
  "requests"
]
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
evrmail = "evrmail.__init__:main"
