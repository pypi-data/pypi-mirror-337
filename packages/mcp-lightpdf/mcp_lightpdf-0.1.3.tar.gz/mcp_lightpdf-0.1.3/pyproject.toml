[project]
name = "mcp-lightpdf"
version = "0.1.3"
description = "LightPDF文档转换工具"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp",
    "httpx",
    "python-dotenv",
]

[project.scripts]
mcp-lightpdf = "mcp_lightpdf:main"
