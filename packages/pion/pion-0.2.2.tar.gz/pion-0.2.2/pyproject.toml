[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pion"
version = "0.2.2"
description = "A Python project for time series data and databases."
authors = [
    { name = "Emin Martinian", email = "emin@aocks.com" }
]
license = { file = "LICENSE" }
readme = "README.md"
dependencies = [
    "requests",
    "numpy",
    "pandas",   
]

[project.urls]
Homepage = "https://github.com/aocks/pion-ts"
Documentation = "https://github.com/aocks/pion-ts"
Repository = "https://github.com/aocks/pion-ts.git"
Issues = "https://github.com/aocks/pion-ts/issues"


[project.optional-dependencies]
dev = [
    "pytest",
    "pylint",
]


[tool.setuptools]
# If there are data files included in your packages that need to be
# installed, specify them here.
package-data = { "sample" = ["*.dat"] }

